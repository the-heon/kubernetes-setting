apiVersion: v1
kind: Service
metadata:
  name: chat-presence-server-service
spec:
  selector:
    app: chat-presence-server
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
