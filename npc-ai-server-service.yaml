apiVersion: v1
kind: Service
metadata:
  name: npc-ai-server-service
spec:
  selector:
    app: npc-ai-server
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
