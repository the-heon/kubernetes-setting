apiVersion: apps/v1
kind: Deployment
metadata:
  name: social-server
  labels:
    app: social-server
spec:
  replicas: 2
  selector:
    matchLabels:
      app: social-server
  template:
    metadata:
      labels:
        app: social-server
    spec:
      containers:
        - name: social-server
          image: daev681/daev681:social-server
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 8080
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
      imagePullSecrets:
        - name: my-secret
