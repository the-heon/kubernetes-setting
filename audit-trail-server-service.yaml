apiVersion: v1
kind: Service
metadata:
  name: audit-trail-server-service
spec:
  selector:
    app: audit-trail-server
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
