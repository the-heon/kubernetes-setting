apiVersion: v1
kind: Service
metadata:
  name: liveops-config-server-service
spec:
  selector:
    app: liveops-config-server
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
