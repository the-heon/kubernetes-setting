apiVersion: v1
kind: Service
metadata:
  name: event-bus-gateway-service
spec:
  selector:
    app: event-bus-gateway
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
