apiVersion: v1
kind: Service
metadata:
  name: session-orchestrator-service
spec:
  selector:
    app: session-orchestrator
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
