apiVersion: apps/v1
kind: Deployment
metadata:
  name: world-zone-server
  labels:
    app: world-zone-server
spec:
  replicas: 2
  selector:
    matchLabels:
      app: world-zone-server
  template:
    metadata:
      labels:
        app: world-zone-server
    spec:
      containers:
        - name: world-zone-server
          image: daev681/daev681:world-zone-server
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 8080
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
      imagePullSecrets:
        - name: my-secret
