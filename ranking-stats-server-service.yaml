apiVersion: v1
kind: Service
metadata:
  name: ranking-stats-server-service
spec:
  selector:
    app: ranking-stats-server
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
