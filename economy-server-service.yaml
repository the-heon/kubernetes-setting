apiVersion: v1
kind: Service
metadata:
  name: economy-server-service
spec:
  selector:
    app: economy-server
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
