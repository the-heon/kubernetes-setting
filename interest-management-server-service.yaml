apiVersion: v1
kind: Service
metadata:
  name: interest-management-server-service
spec:
  selector:
    app: interest-management-server
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
