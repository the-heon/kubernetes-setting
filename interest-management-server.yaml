apiVersion: apps/v1
kind: Deployment
metadata:
  name: interest-management-server
  labels:
    app: interest-management-server
spec:
  replicas: 2
  selector:
    matchLabels:
      app: interest-management-server
  template:
    metadata:
      labels:
        app: interest-management-server
    spec:
      containers:
        - name: interest-management-server
          image: daev681/daev681:interest-management-server
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 8080
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
      imagePullSecrets:
        - name: my-secret
