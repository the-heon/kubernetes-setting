apiVersion: v1
kind: Service
metadata:
  name: quest-scenario-server-service
spec:
  selector:
    app: quest-scenario-server
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
