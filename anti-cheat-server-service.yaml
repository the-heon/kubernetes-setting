apiVersion: v1
kind: Service
metadata:
  name: anti-cheat-server-service
spec:
  selector:
    app: anti-cheat-server
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
