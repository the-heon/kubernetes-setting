apiVersion: apps/v1
kind: Deployment
metadata:
  name: ranking-stats-server
  labels:
    app: ranking-stats-server
spec:
  replicas: 2
  selector:
    matchLabels:
      app: ranking-stats-server
  template:
    metadata:
      labels:
        app: ranking-stats-server
    spec:
      containers:
        - name: ranking-stats-server
          image: daev681/daev681:ranking-stats-server
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 8080
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
      imagePullSecrets:
        - name: my-secret
