apiVersion: v1
kind: Service
metadata:
  name: patch-manifest-server-service
spec:
  selector:
    app: patch-manifest-server
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
