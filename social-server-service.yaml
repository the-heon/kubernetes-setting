apiVersion: v1
kind: Service
metadata:
  name: social-server-service
spec:
  selector:
    app: social-server
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
