apiVersion: v1
kind: Service
metadata:
  name: instance-dungeon-server-service
spec:
  selector:
    app: instance-dungeon-server
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
