apiVersion: apps/v1
kind: Deployment
metadata:
  name: chat-presence-server
  labels:
    app: chat-presence-server
spec:
  replicas: 2
  selector:
    matchLabels:
      app: chat-presence-server
  template:
    metadata:
      labels:
        app: chat-presence-server
    spec:
      containers:
        - name: chat-presence-server
          image: daev681/daev681:chat-presence-server
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 8080
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
      imagePullSecrets:
        - name: my-secret
