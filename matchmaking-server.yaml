apiVersion: apps/v1
kind: Deployment
metadata:
  name: matchmaking-server
  labels:
    app: matchmaking-server
spec:
  replicas: 2
  selector:
    matchLabels:
      app: matchmaking-server
  template:
    metadata:
      labels:
        app: matchmaking-server
    spec:
      containers:
        - name: matchmaking-server
          image: daev681/daev681:matchmaking-server
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 8080
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
      imagePullSecrets:
        - name: my-secret
