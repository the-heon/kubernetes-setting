apiVersion: v1
kind: Service
metadata:
  name: trading-auction-server-service
spec:
  selector:
    app: trading-auction-server
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
