apiVersion: v1
kind: Service
metadata:
  name: cache-proxy-server-service
spec:
  selector:
    app: cache-proxy-server
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
