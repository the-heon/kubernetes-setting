apiVersion: v1
kind: Service
metadata:
  name: world-event-scheduler-server-service
spec:
  selector:
    app: world-event-scheduler-server
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
