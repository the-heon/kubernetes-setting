apiVersion: apps/v1
kind: Deployment
metadata:
  name: npc-ai-server
  labels:
    app: npc-ai-server
spec:
  replicas: 2
  selector:
    matchLabels:
      app: npc-ai-server
  template:
    metadata:
      labels:
        app: npc-ai-server
    spec:
      containers:
        - name: npc-ai-server
          image: daev681/daev681:npc-ai-server
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 8080
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
      imagePullSecrets:
        - name: my-secret
