apiVersion: v1
kind: Service
metadata:
  name: gm-tool-server-service
spec:
  selector:
    app: gm-tool-server
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
